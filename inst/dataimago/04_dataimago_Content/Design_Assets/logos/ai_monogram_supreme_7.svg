<svg width="480" height="480" viewBox="0 0 480 480" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .futura {
        font-family: 'Futura';
        font-style: italic;
        font-size: 400px;
        font-weight: bold;
      }

      .data {
        fill: black;
      }

      .imago {
        font-size: 360px;
      }

      .background {
        fill: white;
      }
    </style>
  </defs>

  <!-- Transparent Background (optional for clarity) -->
  <rect class="background" width="100%" height="100%" rx="5" ry="5" />

  <!-- Animated "a" -->
  <text x="105" y="360" class="futura imago">
    <animate attributeName="fill"
         values="
           #000000;
           #000000;
           #999999;
           #CCCCCC;
           #FFFFFF;
           #FFFFFF;
           #FFFFFF;
           #CCCCCC;
           #999999;
           #000000;
           #000000;
         "
         dur="4s"
         repeatCount="indefinite" />
    a
  </text>

    <!-- Animated "i" — only the dot is visible -->
  <text x="229" y="360" class="futura imago">
    <animate attributeName="fill"
         values="
           rgba(0, 255, 0, 0);
           rgba(0, 255, 0, 0);
           rgba(0, 255, 0, 0.3);
           rgba(0, 255, 0, 0.8);
           rgb(0, 255, 0);
           rgb(0, 0, 255);
           rgb(255, 0, 0);
           rgba(255, 0, 0, 0.8);
           rgba(255, 0, 0, 0.3);
           rgba(255, 0, 0, 0);
           rgba(255, 0, 0, 0);
         "
         dur="4s"
         repeatCount="indefinite" />
    i
  </text>

</svg>