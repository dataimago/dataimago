% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai_spec.R
\name{validate_spec_schema}
\alias{validate_spec_schema}
\title{Validate a spec file against the bundled JSON Schema.}
\usage{
validate_spec_schema(spec_path)
}
\arguments{
\item{spec_path}{Path to a \code{dataimago-spec.yaml}.}
}
\value{
Invisibly: \code{TRUE} when the spec validates, \code{NA} when the engine is
unavailable (with a warning). Aborts with per-error details when the spec
violates the schema.
}
\description{
The Zod schema exported by \verb{@dataimago/spec} in
\code{dataimago-ai/packages/spec} is authoritative.
\code{inst/schemas/dataimago-spec.v1alpha1.schema.json} is generated from it with
the package's \verb{dataimago-spec-json-schema --kind ProjectSpec} bin and bundled
here so \code{ai(spec_path)} validates without Node at R runtime -- the
spec-to-artifact-bridge contract (decision 4).
}
\details{
Engine-gated: runs when \code{jsonvalidate} (Suggests) is installed and its ajv
engine compiles the schema; otherwise it is skipped with a warning and the
structural checks in \code{\link[=validate_spec]{validate_spec()}} remain the only gate. Cross-field
invariants that do not survive \code{zod-to-json-schema} (the Zod \code{superRefine}
rules) are re-checked in R by \code{\link[=validate_spec]{validate_spec()}} regardless.
}
\keyword{internal}
